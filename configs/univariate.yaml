# ==================== Univariate 训练配置 ====================
# 继承 base.yaml，覆盖 univariate 特定参数

defaults:
  - base

training:
  total_epochs: 25                  # univariate 总 epoch 数

data:
  batch_size: 32                    # univariate 批大小
  effective_batch_size: 256         # univariate 有效批大小
